"""One arm: the URScript it is sent, the sockets it is sent over, the servo
backends that stream to it, and the simulated stand-in that answers the same
calls without a controller.

Everything here is about a single arm. Two of them together is `cell.py`.
"""
